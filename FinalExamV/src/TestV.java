import java.util.HashMap;
import java.util.UUID;
import Person
public class TestV {

	public TestV() {
		Person A = new Person();
		Person B = new Person(); 		
		HashMap<UUID, Person> PersonMap = new HashMap<UUID, Person>();
		PersonMap.put(A.getPersonID(), A);
		PersonMap.put(B.getPersonID(), B);
		
		
	}

	public Vehicle transferOwnership(B);
}
